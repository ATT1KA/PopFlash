{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "noEmit": true
  },
  "include": ["packages", "services", "apps", "scripts"],
  "exclude": ["node_modules", "dist", "build"]
}